/*** Bot's Olympics functionality ***/

import { Message } from "discord.js";

/** Register a new submission */
function registerSubmission(uid: number, message: Message) : void {

}

/** Validate the new submission via reaction */
function validateSubmission(message: Message) : boolean {
    return false;
}

/** Get the current board of submissions for users */
function getOlympicsBoard() : JSON {
    return {};
}